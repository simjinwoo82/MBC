# 파비콘 추가 프롬프트

## 요청 내용
`@index.html` 문서에 `@favicon.svg`를 파비콘으로 넣어

## 수행 결과
`index.html`의 `<head>` 섹션에 아래와 같은 `<link>` 태그가 추가되었습니다:

```html
<link rel="icon" type="image/svg+xml" href="favicon.svg" />
```
